
include /includes/_variables
include /includes/_prefab

-
  const styles = [
    //- "./assets/css/contact.min.css",
  ]

-
  const scripts = [
    //- "./assets/js/contact.min.js",
    //- "./assets/js/jquery.min.js",
    //- "./assets/js/swiper.min.js",
  ]

doctype html
html(lang="ja")
  include /includes/_head

  body
    //- include /includes/_trace
    include /_header

    main.-contact
      .l-n-fv
        .l-n-inner.-wide.js-n-fv
          .l-n-fv__main.c-n-view
            h1 お問い合わせ
      .l-n-breadcrumb
        .l-n-inner
          nav.p-n-breadcrumb(aria-label="パンくずリスト")
            ol.-list(itemscope, itemtype="https://schema.org/BreadcrumbList")
              li(itemprop="itemListElement", itemscope, itemtype="https://schema.org/ListItem")
                +anchor('index.html')(class="-underline", itemprop="item")
                  span(itemprop="name") トップ
                meta(itemprop="position", content="1")
              li(itemprop="itemListElement", itemscope, itemtype="https://schema.org/ListItem")
                span(itemprop="name") お問い合わせ
                meta(itemprop="position", content="2")
      .l-n-contact
        .l-n-inner
          h2.u-n-contact-title 送信いただき<br class="-only-sp">ありがとうございます
          p.u-n-contact-text02 受付が完了しました。<br><br>内容を確認し、<br class="-only-sp">折り返しご連絡いたしますので<br class="-only-sp">しばらくお待ちください。<br>何卒よろしくお願い申し上げます。
          +anchor("index.html")(class="u-n-contact-button c-n-button -opacity") トップに戻る

    include /_footer
